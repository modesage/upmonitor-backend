{
  "name": "worker",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun index.ts",
    "start": "bun index.ts",
    "build": "bun install && cd ../../packages/store && npx prisma generate && cd ../../apps/worker"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "express": "^5.1.0",
    "redisstream": "workspace:*",
    "store": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
