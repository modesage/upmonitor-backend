{
  "name": "pusher",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun index.ts",
    "start": "bun index.ts",
    "build": "bun install && cd ../../packages/store && npx prisma generate && cd ../../apps/pusher"
  },
  "dependencies": {
    "store": "workspace:*",
    "redisstream": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
